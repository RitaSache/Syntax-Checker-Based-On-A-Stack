/*#include <iostream>
#include "Syntax.h"
using namespace std;*/

/*bool Syntax::isClosed(char curr) {
	current = curr;
	if (curr == '(') {
		return true;
	}
	else if (curr == '{') {
		return true;
	}

	else if (curr == '[') {
		return true;
	}

	else {
		return false;
	}
}*/

/*char Syntax::opposite(char current) {
	current = current;
	char opposite;
	if (current == '(') {
		opposite = ')';
	}
	else if (current == '{') {
		opposite = '}';
	}
	else {
		opposite = ']';
	}
	return opposite;

}*/